namespace QuickPulse.Tests;

public class Spike_Actions
{
    // [Fact]
    // public void Times()
    // {
    //     var calls = 0;
    //     Signal.From<Unit>(a => Pulse.Trace(calls++))
    //         .Pulse(Enumerable.Repeat(Unit.Instance, 5));
    //     Assert.Equal(5, calls);
    // }

    // [Fact]
    // public void TimesUntil()
    // {
    //     var calls = 0;
    //     Signal.From<Unit>(a =>
    //             Pulse.StopFlowingIf(calls == 1).Then(Pulse.Trace(calls++)))
    //         .Pulse(Enumerable.Repeat(Unit.Instance, 5));
    //     Assert.Equal(2, calls);
    // }
}